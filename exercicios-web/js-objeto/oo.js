// CÓDIGO NÃO EXECUTÁVEL!!!

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes:
// 1. abstracao -> traduzir do real para o código, conforme exigências do contexto
// 2. encapsulamento -> Detalhes de implementação escondidos, não necessariamente mudando a interface (forma de usar), 
// alto acoplamento (baixo encapsulamento) - alta dependência - menor escalabilidade
// 3. herança (prototype)
// 4. polimorfismo